# Server (Netlify Functions) — copy thành `.env` ở gốc repo (cùng cấp netlify.toml)

SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
PUBLIC_BASE_URL=http://localhost:8888
LEADER_EMAILS=
CORS_ORIGINS=http://localhost:8888

# Tuỳ chọn
# RESEND_API_KEY=
# HR_EMAILS=
# FIREBASE_SERVICE_ACCOUNT_JSON=
